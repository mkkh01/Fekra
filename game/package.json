{
  "name": "siyar-al-mamalik",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "سير الممالك — نموذج أولي مرئي للعبة استراتيجية حربية (مقاطعات + Canvas + TypeScript)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
